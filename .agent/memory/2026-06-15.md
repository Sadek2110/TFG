# 2026-06-15 - Preparación para Despliegue en Producción (Easypanel)

## Contexto y Cambios Realizados
1. **Configuración Dinámica:**
   - Modificado [configuracion.php](file:///c:/Users/sadek/Desktop/Proyectos%20personales/RefactorizarTFG/Fastplay/config/configuracion.php) para usar `define` en lugar de `const` para `RUTA_BD` y `RUTA_SESIONES`, permitiendo usar `getenv()` y haciendo que las rutas de la base de datos y de las sesiones sean configurables externamente en entornos de producción (`FASTPLAY_BD_PATH` y `FASTPLAY_SESIONES_PATH`).
2. **Dockerfile para Producción:**
   - Creado un [Dockerfile](file:///c:/Users/sadek/Desktop/Proyectos%20personales/RefactorizarTFG/Fastplay/Dockerfile) en la raíz del proyecto para empaquetar la aplicación usando `php:8.1-apache`, instalando las extensiones necesarias (`pdo_sqlite`), configurando `mod_rewrite`, apuntando el `DocumentRoot` a `/public` para mayor seguridad, y otorgando permisos de escritura adecuados al servidor web en `/var/www/html/almacenamiento/`.
3. **Validación:**
   - Se corrieron las pruebas unitarias mediante `php tests/correr.php` verificando que las modificaciones no rompen la lógica actual y siguen reportando `16 OK / 0 KO`.
