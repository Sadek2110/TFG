// Interacciones del hero de la portada.
//
//   - Cabecera transparente sobre el hero que se vuelve opaca al hacer scroll,
//     para que el logotipo y los enlaces sigan siendo legibles sobre el vídeo
//     cuando el usuario ya ha pasado la primera pantalla.
//
// Se ejecuta solo si encuentra un .hero y una .cabecera en la página (en el
// resto de páginas el módulo se sale sin tocar nada).

(function () {
    'use strict';
    // La cabecera ya tiene su propio fondo translúcido que funciona sobre cualquier
    // fondo (claro u oscuro). No necesitamos tocarla con JavaScript.
})();
